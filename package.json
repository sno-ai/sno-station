{
	"name": "sno-station",
	"private": true,
	"license": "Apache-2.0",
	"packageManager": "npm@11.9.0",
	"workspaces": [
		"apps/*",
		"packages/*",
		"!packages/chunking",
		"!packages/content-sanitizer"
	],
	"engines": {
		"node": ">=24.15.0 <25"
	},
	"scripts": {
		"build": "npm run build --workspace @snoai/common-core && npm run build --workspace @snoai/utils && npm run build --workspace @snoai/embedder && npm run build --workspace @snoai/sno-observe && npm run build --workspace @snoai/sno-station-core-crypto && npm run build --workspace @snoai/sno-station-mem && npm run build --workspace @snoai/mem-claw && npm run build --workspace @snoai/mem-claude && npm run build --workspace @snoai/mem-codex",
		"typecheck": "npm run typecheck --workspaces --if-present",
		"lint": "npm run lint --workspaces --if-present"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.0.0",
		"@types/node": "^22.19.17",
		"better-sqlite3": "^13.0.3",
		"drizzle-kit": "^0.31.11",
		"drizzle-orm": "^0.45.3",
		"esbuild": "^0.28.1",
		"hono": "^4.12.18",
		"tar-stream": "3.2.1",
		"tsx": "^4.23.15",
		"typescript": "^7.0.2",
		"vitest": "^4.1.11"
	},
	"overrides": {
		"@esbuild-kit/core-utils": {
			"esbuild": "0.25.12"
		},
		"sharp": "0.35.4"
	}
}
